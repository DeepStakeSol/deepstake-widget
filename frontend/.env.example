# Optional build identifier reported by mount telemetry; package.json version is the fallback.
VITE_WIDGET_VERSION=

# Absolute collector URL embedded into the widget build.
VITE_TELEMETRY_ENDPOINT=https://deepstake.info/api/telemetry
